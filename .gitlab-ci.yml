image: node

before_script:
  - node --version
  - npm --version
  - npm ci || npm install

package:
  script:
    - npm run make
  artifacts:
    paths:
      - dist/

#pages:
#  stage: deploy
#  script:
#    - npm run make
#  environment:
#    name: Production
#    url: https://example-angular-bootstrap.tripetto.com/
#  artifacts:
#    paths:
#      - dist
#  only:
#    - master
